server:
  port: 9999

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_manager
    username: paytak
    password: code
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create