server:
  port: 9999

spring:
  datasource:
    url: jdbc:mysql://inventory-manager.canmygoaixjp.ap-northeast-2.rds.amazonaws.com:3306/inventory_manager
    username: paytakcode
    password: paytakcode
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    com.paytakcode.inventorymanager.api.v1: DEBUG


jwt:
  secret: paytak.code.first.personal.project
  duration: 32400000 # 9 hour